---
export interface Props {
	title: string;
}

const { title } = Astro.props as Props;
---

<!DOCTYPE html>
<div class="section-heading">
	<h3> {title} </h3>
</div>
<style>
	.section-heading {
		font-weight: bold;
		font-size: 1.5rem;
		padding: 1.25rem;
		margin: -0.25rem;
	}
</style>
